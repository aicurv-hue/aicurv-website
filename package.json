{
  "name": "aicurv-website",
  "version": "1.0.0",
  "description": "AICURV - AI-Augmented Software Studio",
  "private": true,
  "scripts": {
    "dev": "python -m http.server 8000 || python3 -m http.server 8000",
    "lint": "echo 'No linter configured for static HTML'",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.3.1"
  }
}
